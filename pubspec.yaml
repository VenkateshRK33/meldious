name: disaster_response_app
description: A disaster response app for real-time incident reporting and resources.

publish_to: 'none' # Remove this line if you want to publish your package.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_maps_flutter: ^2.1.1  # Use the latest stable version
  http: ^0.13.4  # Version for making HTTP requests

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
